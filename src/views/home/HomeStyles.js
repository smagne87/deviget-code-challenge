import * as colors from '../../styles/js/global/colors';

const styles = {
  homeMainContainer: {
    width: '100%',
    height: '100%',
  },
  container: {
    backgroundColor: colors.white,
  },
};

export default styles;
