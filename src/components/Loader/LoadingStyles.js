import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  loaderContainer: {
    position: 'absolute',
    top: '40%',
    left: '40%',
  },
}));

export default useStyles;
